
to build previous type : git clone https://github.com/QEMU-team-for-Galaxy-S2-Emulation/previous.git

then make a build folder in MSYS2 MINGW64
then type ../configure in build folder
let it run
then type ninja
it will make then copy libpcap.dll from my .zip file
also before making it install dependicies otherwise it will make errors
i hope it works
Thank You
by Hydra
